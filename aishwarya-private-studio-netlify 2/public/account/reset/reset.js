(() => { const app = document.querySelector('#resetApp'); const token = new URLSearchParams(location.search).get('token'); const api = async body => { const response = await fetch('/.netlify/functions/admin?action=customer/password-reset/complete', { method:'POST', headers:{'content-type':'application/json'}, body:JSON.stringify(body) }); const data = await response.json(); if (!response.ok) throw new Error(data.error || 'Please try again.'); return data; }; if (!token) { app.innerHTML = '<h1>Reset link <em>missing.</em></h1><p class="account-copy">Please request a new password reset link.</p><a class="button full" href="/account/">Back to account <span>→</span></a>'; return; } app.innerHTML = `<p class="eyebrow">Password reset</p><h1>Create a new <em>password.</em></h1><p class="account-copy">Choose a password with at least 8 characters.</p><form id="reset"><label>New password<input type="password" name="password" required minlength="8" autocomplete="new-password"></label><label>Confirm password<input type="password" name="confirm" required minlength="8" autocomplete="new-password"></label><button class="button full">Update password <span>→</span></button></form><p class="account-message"></p>`; app.querySelector('#reset').onsubmit = async event => { event.preventDefault(); const data = Object.fromEntries(new FormData(event.currentTarget)); const msg = app.querySelector('.account-message'); if (data.password !== data.confirm) { msg.textContent = 'Passwords do not match.'; return; } const button = event.submitter; button.disabled = true; button.textContent = 'Updating…'; try { await api({ token, password:data.password }); app.innerHTML = '<p class="eyebrow">Password updated</p><h1>You are all <em>set.</em></h1><p class="account-copy">Your password has been updated. Please sign in.</p><a class="button full" href="/account/">Sign in <span>→</span></a>'; } catch (error) { msg.textContent = error.message; button.disabled = false; button.innerHTML = 'Update password <span>→</span>'; } }; })();
